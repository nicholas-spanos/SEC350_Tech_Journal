Add new user:
  configure
  set system login user nick
  set system login login user nick authentication plaintext-password <password>

Create an rsa key-pair:
  ssh-keygen -t rsa
  
  afterwards, run:
  ssh-copy-id <username>@<ip>
